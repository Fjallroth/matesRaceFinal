// /routes/userRoutes.js
import express from "express";
// import { updateUserProfile } from '../controllers/userController.js'; // Example
// import { isAuthenticated } from '../middleware/authMiddleware.js'; // Example

const router = express.Router();

// router.put('/profile', isAuthenticated, updateUserProfile); // Example future route

export default router;
